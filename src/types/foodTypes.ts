export type Food = {
  avatar: string;
  id: string;
  createdAt: string;
  name: string;
};

export type CartFood = {
  avatar: string;
  id: string;
  createdAt: string;
  name: string;
  quantity: number;
};
